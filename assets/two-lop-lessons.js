(function(){
"use strict";

const LESSONS={
 1018:{estimate:{lat:36+17.7/60,lon:-(24+54.5/60),label:"36°17,7′ N · 024°54,5′ W"},stars:[{name:"Kochab",az:345,azLabel:"345°",dh:-2},{name:"Arcturus",az:248,azLabel:"248°",dh:3}],result:"36°15,0′ N · 024°57,2′ W",answer:"D"},
 1099:{estimate:{lat:27,lon:-(16+31.9/60),label:"27°00,0′ N · 016°31,9′ W"},stars:[{name:"Atria",az:5,azLabel:"005°",dh:-3.5},{name:"Fomalhaut",az:120,azLabel:"120°",dh:2.5}],result:"26°56,4′ N · 016°31,0′ W",answer:"A"},
 1137:{estimate:{lat:24+50.3/60,lon:150+58/60,label:"24°50,3′ N · 150°58,0′ E"},stars:[{name:"Kochab",az:346,azLabel:"N14W = 346°",dh:3},{name:"Hamal",az:75,azLabel:"N75E = 075°",dh:2}],result:"24°53,7′ N · 150°59,3′ E",answer:"A"},
 1177:{estimate:{lat:33+5/60,lon:-(61+27.7/60),label:"33°05,0′ N · 061°27,7′ W"},stars:[{name:"Markab",az:85,azLabel:"085°",dh:-2},{name:"Dubhe",az:330.5,azLabel:"330,5°",dh:-.5}],result:"33°03,4′ N · 061°30,0′ W",answer:"C"},
 1217:{estimate:{lat:-(15+22/60),lon:113+45/60,label:"15°22,0′ S · 113°45,0′ E"},stars:[{name:"Hamal",az:344.5,azLabel:"N15,5W = 344,5°",dh:3.3},{name:"Fomalhaut",az:244.5,azLabel:"S64,5W = 244,5°",dh:-2.8}],result:"15°18,2′ S · 113°46,4′ E",answer:"A"},
 179:{estimate:{lat:33+26.1/60,lon:-(60+53.2/60),label:"33°26,1′ N · 060°53,2′ W"},stars:[{name:"Sirius",az:198,azLabel:"198°",dh:5},{name:"Betelgeuse",az:230,azLabel:"230°",dh:5}],result:"33°21,8′ N · 060°56,9′ W",answer:"C"},
 299:{estimate:{lat:31+52.1/60,lon:-(65+18.8/60),label:"31°52,1′ N · 065°18,8′ W"},stars:[{name:"Altaír",az:137,azLabel:"137°",dh:7},{name:"Alphecca",az:274,azLabel:"274°",dh:-3}],result:"31°44,9′ N · 065°16,0′ W",answer:"B"},
 617:{estimate:{lat:35+5/60,lon:-(7+32/60),label:"35°05,0′ N · 007°32,0′ W"},stars:[{name:"Rigel",az:235,azLabel:"235°",dh:-5},{name:"Dubhe",az:30,azLabel:"030°",dh:3}],result:"35°04,9′ N · 007°24,5′ W",answer:"—",sourceConflict:true},
 778:{estimate:{lat:22+20.6/60,lon:-(36+10.4/60),label:"22°20,6′ N · 036°10,4′ W"},stars:[{name:"Deneb",az:311,azLabel:"311°",dh:-5},{name:"Rigel",az:107,azLabel:"107°",dh:3}],result:"22°14,3′ N · 036°09,0′ W",answer:"A"},
 854:{estimate:{lat:23+27.8/60,lon:-(41+47.2/60),label:"23°27,8′ N · 041°47,2′ W"},stars:[{name:"Pollux",az:321,azLabel:"321°",dh:5},{name:"Arcturus",az:82,azLabel:"082°",dh:2}],result:"23°34,9′ N · 041°46,0′ W",answer:"D"},
 896:{estimate:{lat:30+10/60,lon:-(40+20/60),label:"30°10,0′ N · 040°20,0′ W"},stars:[{name:"Altaír",az:125,azLabel:"125°",dh:4},{name:"Regulus",az:235,azLabel:"235°",dh:5}],result:"30°03,2′ N · 040°20,8′ W",answer:"C",graphicalDiscrepancy:true}
};

Object.keys(LESSONS).forEach(id=>window.RECLASSIFIED_LESSON_IDS?.add(Number(id)));

const baseType=window.reclassifiedTypeHTML;
const baseSolution=window.reclassifiedSolutionHTML;
const baseQuick=window.reclassifiedQuickHTML;
const baseGraphics=window.initReclassifiedGraphics;
const comma=value=>String(value).replace(".",",");
const signed=value=>`${value>=0?"+":"−"}${comma(Math.abs(value).toFixed(1))}′`;
const signedMiles=value=>`${value>=0?"+":"−"}${comma(Math.abs(value).toFixed(1))} millas`;
const direction=value=>value>=0?"hacia el astro":"en sentido contrario al astro";
const opposite=az=>(az+180)%360;
const solve=s=>{
 const r=Math.PI/180,[a,b]=s.stars,c1=Math.cos(a.az*r),n1=Math.sin(a.az*r),c2=Math.cos(b.az*r),n2=Math.sin(b.az*r),det=c1*n2-c2*n1;
 const north=(a.dh*n2-b.dh*n1)/det,east=(c1*b.dh-c2*a.dh)/det,lonMinutes=east/Math.cos(Math.abs(s.estimate.lat)*r);
 return {north,east,lonMinutes};
};
const minuteText=value=>comma(Number(value).toFixed(1)).padStart(4,"0");
const latitudeParts=s=>{const match=s.estimate.label.match(/^(\d+)°(\d+(?:,\d+)?)′/);return match?[match[1],minuteText(match[2].replace(",","."))]:[String(Math.trunc(Math.abs(s.estimate.lat))),minuteText((Math.abs(s.estimate.lat)%1)*60)]};
const decimalKeys=value=>String(value).replace(",",".").split("");
const angleKeys=parts=>decimalKeys(parts[0]).concat(["°′″"],decimalKeys(parts[1]),["°′″"]);
const coordinateParts=value=>[String(Math.floor(Math.abs(value)+1e-9)),minuteText((Math.abs(value)%1)*60)];
const coordinateText=(value,pad=2)=>{let[degrees,minutes]=coordinateParts(value),d=Number(degrees),m=Number(minutes.replace(",","."));if(m>=60){d++;m=0}return `${String(d).padStart(pad,"0")}°${minuteText(m)}′`};
const coordinateDms=(value,pad=2)=>{let total=Math.round(Math.abs(value)*3600),degrees=Math.floor(total/3600);total-=degrees*3600;const minutes=Math.floor(total/60),seconds=total-minutes*60;return `${String(degrees).padStart(pad,"0")}°${String(minutes).padStart(2,"0")}′${String(seconds).padStart(2,"0")}″`};
const calculatorGuide=(id,s)=>{
 const v=solve(s),miles=Math.abs(v.east).toFixed(2),lat=latitudeParts(s),lon=coordinateParts(s.estimate.lon),north=Number(Math.abs(v.north).toFixed(1)),longitude=Number(Math.abs(v.lonMinutes).toFixed(1)),latOp=s.estimate.lat*v.north>=0?"+":"−",lonOp=s.estimate.lon*v.east>=0?"+":"−",latFinal=Math.abs(s.estimate.lat)+(latOp==="+"?north:-north)/60,lonFinal=Math.abs(s.estimate.lon)+(lonOp==="+"?longitude:-longitude)/60,first=decimalKeys(miles).concat(["÷","cos"],angleKeys(lat),[")","="]),second=["AC"].concat(angleKeys(lat),[latOp],angleKeys(["0",comma(north.toFixed(1))]),["="]),third=["AC"].concat(angleKeys(lon),[lonOp],angleKeys(["0",comma(longitude.toFixed(1))]),["="]),expected=first.concat(second,third),directions=expected.map(key=>`Pulsa ${key}.`),milesEnd=decimalKeys(miles).length,firstEnd=first.length,secondEnd=first.length+second.length;
 directions[0]=`Del dibujo has medido ${comma(miles)} millas en dirección Este/Oeste. Empieza escribiendo ${comma(miles)}.`;
 directions[milesEnd]="Pulsa ÷: vamos a convertir esas millas en minutos de longitud.";
 directions[milesEnd+1]=`Pulsa COS. La calculadora abrirá cos( y ahora introducirás la latitud estimada ${lat[0]}°${lat[1]}′.`;
 directions[milesEnd+2]=`Escribe la latitud estimada ${lat[0]}°${lat[1]}′ exactamente como aparece en el enunciado.`;
 directions[firstEnd-2]="Cierra el paréntesis del coseno.";
 directions[firstEnd-1]="Pulsa = para obtener cuántos minutos de longitud corresponden al desplazamiento Este/Oeste.";
 directions[firstEnd]="Pulsa AC para comenzar la cuenta de la latitud observada.";
 directions[firstEnd+1]=`Escribe la latitud estimada ${lat[0]}°${lat[1]}′.`;
 directions[firstEnd+angleKeys(lat).length+1]=`Como el cruce queda hacia el ${v.north>=0?"Norte":"Sur"}, pulsa ${latOp}: el número de la latitud ${latOp==="+"?"aumenta":"disminuye"}.`;
 directions[secondEnd]="Pulsa AC para comenzar la cuenta de la longitud observada.";
 directions[secondEnd+1]=`Escribe la longitud estimada ${lon[0]}°${lon[1]}′.`;
 directions[secondEnd+angleKeys(lon).length+1]=`Como el cruce queda hacia el ${v.east>=0?"Este":"Oeste"}, pulsa ${lonOp}: el número de la longitud ${lonOp==="+"?"aumenta":"disminuye"}.`;
 return {expected,directions,formulaEnd:firstEnd,inverseEnd:secondEnd,milestones:[{at:firstEnd,value:Math.abs(v.lonMinutes).toFixed(9)},{at:secondEnd,value:coordinateDms(latFinal,2)},{at:expected.length,value:coordinateDms(lonFinal,3)}],storageKey:`navegandoAndo_twoLop_${id}_v2`,finalText:`situación observada ≈ ${s.result}.`,subtitle:`Convierte el desplazamiento y aplícalo a la situación estimada.`,objective:`Objetivo: pasar del cruce del dibujo a la situación observada`,initial:`Empieza por las ${comma(miles)} millas Este/Oeste obtenidas al medir el dibujo.`,firstFeedback:`ΔL = ${comma(Math.abs(v.lonMinutes).toFixed(3))}′, que redondeada a una décima es ${comma(longitude.toFixed(1))}′. Ahora aplícala junto con Δl a la situación estimada.`,secondFeedback:`Latitud observada: ${coordinateText(latFinal,2)} ${latFinal>=0?"N":"S"}. Ahora calcula la longitud observada.`,steps:"<li>Convertir las millas Este/Oeste en minutos de longitud.</li><li>Aplicar el desplazamiento Norte/Sur a la latitud estimada.</li><li>Aplicar el desplazamiento Este/Oeste a la longitud estimada.</li>"};
};
window.RECLASSIFIED_CALCULATOR_GUIDES=window.RECLASSIFIED_CALCULATOR_GUIDES||{};
Object.entries(LESSONS).forEach(([id,s])=>{window.RECLASSIFIED_CALCULATOR_GUIDES[`two-lop-${id}`]=calculatorGuide(id,s)});
const moveLabel=(value,positive,negative)=>`${comma(Math.abs(value).toFixed(1))} millas hacia el ${value>=0?positive:negative}`;
const markText=star=>star.dh>=0?`avanza ${comma(Math.abs(star.dh))} millas por el azimut ${star.azLabel}, hacia ${star.name}`:`avanza ${comma(Math.abs(star.dh))} millas en sentido contrario al azimut ${star.azLabel}; equivale al rumbo ${comma(opposite(star.az))}°`;

window.reclassifiedTypeHTML=function(q){
 const s=LESSONS[q.id];if(!s)return baseType(q);
 return `<div class="context-lesson"><div class="context-card context-type"><h3>TIPO DE PROBLEMA · DOS RECTAS DE ALTURA · SITUACIÓN OBSERVADA</h3><p><b>Cómo reconocerlo:</b> aparecen una situación estimada y dos determinantes. Cada determinante contiene el azimut verdadero <b>Zv</b> y la diferencia de alturas <b>Δa = av − ae</b>.</p><div class="context-note"><b>¿Necesitas Almanaque?</b> No. Los determinantes ya están calculados. Aquí solo debes trasladarlos al papel, trazar las dos perpendiculares y localizar su cruce.</div><div class="context-note"><b>La idea esencial:</b> la situación estimada es el punto de partida provisional; la situación observada es el punto donde se cruzan las dos rectas.</div></div></div>`;
};

function formulaRow(symbol,label,formula,result){return `<div class="context-formula"><div class="context-formula-left"><small>${label}</small><b>${symbol}</b></div><div class="context-formula-eq">=</div><div class="context-formula-right"><small>${formula}</small><b>${result}</b></div></div>`}
function coordinateFormulaRow(symbol,label,formula){return `<div class="context-formula"><div class="context-formula-left"><small>${label}</small><b>${symbol}</b></div><div class="context-formula-eq">=</div><div class="context-formula-right"><b>${formula}</b></div></div>`}
function starInstruction(star,index){const color=index?"green":"";return `<div class="context-step ${color}"><span class="context-step-label">PASO ${index+2}</span><b>DIBUJA LA RECTA DE ${star.name.toUpperCase()}</b><ol><li>Desde la situación estimada traza el azimut <b>${star.azLabel}</b>.</li><li>Como <b>Δa = ${signed(star.dh)}</b>, ${markText(star)}.</li><li>Por esa marca dibuja una línea que corte el azimut a <b>90°</b>. Esa perpendicular es la recta de altura de ${star.name}.</li></ol></div>`}
function solutionHTML(q,s){
 const hasCaution=s.sourceConflict||s.graphicalDiscrepancy,v=solve(s),ns=moveLabel(v.north,"Norte","Sur"),ew=moveLabel(v.east,"Este","Oeste"),latDir=v.north>=0?"Norte":"Sur",lonDir=v.east>=0?"Este":"Oeste",lonShift=Math.abs(v.lonMinutes),latParts=latitudeParts(s),latText=`${latParts[0]}°${latParts[1]}′`,eastWestMiles=comma(Math.abs(v.east).toFixed(2)),status=hasCaution?"#d6a31c":"#58a66c",bg=hasCaution?"#fffaf0":"#f3fbf5",latHem=s.estimate.lat>=0?"N":"S",lonHem=s.estimate.lon>=0?"E":"W",latMove=Math.abs(v.north),lonMove=Math.abs(v.lonMinutes),latRounded=Number(latMove.toFixed(1)),lonRounded=Number(lonMove.toFixed(1)),latOp=s.estimate.lat*v.north>=0?"+":"−",lonOp=s.estimate.lon*v.east>=0?"+":"−",latObserved=Math.abs(s.estimate.lat)+(latOp==="+"?latRounded:-latRounded)/60,lonObserved=Math.abs(s.estimate.lon)+(lonOp==="+"?lonRounded:-lonRounded)/60,latStart=coordinateText(s.estimate.lat,2),lonStart=coordinateText(s.estimate.lon,3),latEnd=coordinateText(latObserved,2),lonEnd=coordinateText(lonObserved,3),latWhy=latOp==="+"?`El cruce queda hacia el ${latDir}, el mismo hemisferio de la latitud ${latHem}; nos alejamos del ecuador y el número aumenta.`:`El cruce queda hacia el ${latDir}, en dirección al ecuador desde la latitud ${latHem}; nos acercamos al ecuador y el número disminuye.`,lonWhy=lonOp==="+"?`El cruce queda hacia el ${lonDir}, el mismo lado de la longitud ${lonHem}; nos alejamos de Greenwich y el número aumenta.`:`El cruce queda hacia el ${lonDir}, en dirección a Greenwich desde la longitud ${lonHem}; nos acercamos a Greenwich y el número disminuye.`;
 const conflict=s.sourceConflict?`<div class="context-check" style="border-color:#d6a31c;background:#fff8df"><b>INCOHERENCIA DEL EXAMEN ORIGINAL:</b> el enunciado oficial impreso da una estima de <b>35°05′ N</b>, pero todas las opciones comienzan por <b>33°</b>. La construcción matemática produce aproximadamente <b>${s.result}</b>, que no coincide con ninguna opción. Por rigor, esta pregunta queda señalada y no se marca verde hasta localizar una edición oficial corregida.</div>`:s.graphicalDiscrepancy?`<div class="context-check" style="border-color:#d6a31c;background:#fff8df"><b>PRECISIÓN DEL ORIGINAL:</b> la intersección matemática de los determinantes queda aproximadamente en <b>${latEnd} ${latHem} · ${lonEnd} ${lonHem}</b>. La plantilla oficial señala la opción <b>${s.answer}</b>, <b>${s.result}</b>, que es la única opción cercana pero difiere aproximadamente 1′ en latitud. Se conserva la respuesta oficial y se deja visible esta diferencia para no enseñar una cuenta falsa.</div>`:`<div class="context-check"><b>Comprobación independiente:</b> al resolver las dos ecuaciones de las rectas obtenemos ΔN = ${signedMiles(v.north)} y ΔE = ${signedMiles(v.east)}. Tras convertir la componente Este/Oeste a minutos de longitud, la construcción es compatible con la opción <b>${s.answer}</b>.</div>`;
 return `<details class="card" open style="margin-top:10px;background:${bg};border:3px solid ${status}"><summary><b>${s.sourceConflict?"⚠️ Resolución auditada · el original contiene una contradicción":s.graphicalDiscrepancy?"⚠️ Resolución auditada · diferencia en la plantilla oficial":"🟢 Resolución completa · comprobada varias veces"}</b></summary><div class="context-lesson">
 <div class="context-step blue"><span class="context-step-label">PASO 1</span><b>COPIA LA SITUACIÓN ESTIMADA Y LOS DOS DETERMINANTES</b><div class="context-data-grid"><div class="context-data"><span>SITUACIÓN ESTIMADA</span><b>${s.estimate.label}</b></div>${s.stars.map(star=>`<div class="context-data"><span>${star.name.toUpperCase()}</span><b>Zv = ${star.azLabel} · Δa = ${signed(star.dh)}</b></div>`).join("")}</div><div class="context-note"><b>Regla de signos:</b> Δa positiva → marca hacia el astro. Δa negativa → marca la misma distancia en el sentido contrario al astro. El signo decide el sentido; nunca se borra ni se ignora.</div></div>
 ${starInstruction(s.stars[0],0)}${starInstruction(s.stars[1],1)}
 <div class="context-result"><b>PUNTO VERDE = SITUACIÓN OBSERVADA: el barco debe estar sobre las dos rectas a la vez; por eso su posición real es el cruce.</b></div>
 <div class="context-visual lop-visual" data-generic-lop="${q.id}"><h3>EXPLICACIÓN ANIMADA · construye las dos rectas sobre papel</h3><p>Avanza paso a paso. El dibujo usa exactamente los azimuts y las diferencias de altura de esta pregunta.</p><div class="lop-player" aria-live="polite"><div class="lop-stage-count"><span data-generic-lop-stage>PASO VISUAL 1</span><div data-generic-lop-dots aria-hidden="true"></div></div><b data-generic-lop-title>Situación estimada</b><p data-generic-lop-text>El punto azul oscuro es el lugar provisional desde el que empiezan todas las medidas.</p></div><canvas data-generic-lop-canvas aria-label="Construcción animada de dos rectas de altura"></canvas><div class="lop-playback"><button type="button" data-generic-lop-play>▶ Ver explicación</button><button type="button" data-generic-lop-prev>← Anterior</button><button type="button" data-generic-lop-next>Siguiente →</button><button type="button" data-generic-lop-replay>↻ Repetir</button></div><div class="context-controls"><label>Escala <input data-generic-lop-zoom type="range" min="24" max="62" value="38" step="1"></label><button type="button" data-generic-lop-reset>Volver a centrar</button></div><div class="context-note"><b>En el examen:</b> puedes hacer esta construcción en papel en blanco con regla y transportador. La cuadrícula representa millas náuticas; cada marca pequeña vale una milla.</div></div>
 <div class="context-step purple" data-calculation-zone><span class="context-step-label">PASO 4</span><b>LEE EL DESPLAZAMIENTO DEL CRUCE Y PÁSALO A COORDENADAS</b><p>Desde la situación estimada hasta el punto verde se obtienen aproximadamente <b>${ns}</b> y <b>${ew}</b>.</p>${formulaRow("Δl","CAMBIO DE LATITUD",`1 milla Norte/Sur = 1′ de latitud`,`${comma(latMove.toFixed(1))}′ hacia el ${latDir}`)}<div class="context-note"><b>Antes de calcular ΔL, identifica los dos números:</b><ol><li><b>${eastWestMiles} millas</b> no aparece escrito en el enunciado: es la separación horizontal que acabas de medir entre la situación estimada y el punto verde.</li><li><b>${latText}</b> es la latitud estimada que aparece en el enunciado y que introducirás directamente en la calculadora.</li></ol></div>${formulaRow("ΔL","CAMBIO DE LONGITUD",`${eastWestMiles} millas ÷ cos(${latText})`,`${comma(lonShift.toFixed(3))}′ ≈ ${comma(lonShift.toFixed(1))}′ hacia el ${lonDir}`)}<div class="context-note"><b>¿Por qué se divide por el coseno?</b> En latitud, 1 milla siempre equivale a 1′. En longitud no: los meridianos se acercan al alejarnos del ecuador. A la latitud ${latText}, un minuto de longitud mide menos de una milla. Por eso dividimos las ${eastWestMiles} millas por <b>cos(${latText})</b> para averiguar cuántos minutos de longitud representan. No estamos dividiendo por la latitud, sino por su coseno.</div><div class="calc-trainer" data-calculator-trainer data-trainer-instance="pregunta-${q.id}" data-height-guide="two-lop-${q.id}"></div></div>
 <div class="context-step green"><span class="context-step-label">PASO 5</span><b>APLICA LOS DOS DESPLAZAMIENTOS A LA SITUACIÓN ESTIMADA</b><p>Ahora sí terminamos el ejercicio: partimos de las coordenadas estimadas y aplicamos los minutos que acabamos de obtener.</p>${coordinateFormulaRow("l obs","LATITUD OBSERVADA",`${latStart} ${latHem} ${latOp} 0°${minuteText(latRounded)}′ = ${latEnd} ${latHem}`)}<div class="context-note"><b>Por qué ${latOp==="+"?"sumamos":"restamos"} la latitud:</b> ${latWhy}</div>${coordinateFormulaRow("L obs","LONGITUD OBSERVADA",`${lonStart} ${lonHem} ${lonOp} 0°${minuteText(lonRounded)}′ = ${lonEnd} ${lonHem}`)}<div class="context-note"><b>Por qué ${lonOp==="+"?"sumamos":"restamos"} la longitud:</b> ${lonWhy}<br><br><b>Regla que sirve siempre:</b> si el desplazamiento te acerca al ecuador o a Greenwich, el número de la coordenada disminuye; si te aleja, aumenta. El sentido Norte, Sur, Este u Oeste por sí solo no decide el signo: también importa el hemisferio de partida.</div><div class="context-note"><b>Precisión del dibujo:</b> estas cantidades se obtienen midiendo una construcción gráfica. Una pequeña diferencia al redondear es normal; por eso se compara el cruce con las opciones. Si la discrepancia fuese mayor de lo razonable, la pregunta quedará señalada y no se fingirá una coincidencia.</div><div class="context-result"><b>${s.sourceConflict?"Resultado matemático":"Respuesta oficial"}: ${s.result}${s.sourceConflict?"":" · opción "+s.answer}</b></div></div>${conflict}</div></details>`;
}

window.reclassifiedSolutionHTML=q=>LESSONS[q.id]?solutionHTML(q,LESSONS[q.id]):baseSolution(q);
window.reclassifiedQuickHTML=q=>{const s=LESSONS[q.id];if(!s)return baseQuick(q);return `<div class="context-quick"><b>Recordatorio rápido:</b> Δa + va hacia el astro; Δa − va en sentido contrario. Por cada marca traza la perpendicular al azimut; el cruce es la situación observada: <b>${s.result}</b>${s.sourceConflict?" (original contradictorio)":" ("+s.answer+")"}.</div>`};

function canvasSize(canvas,ratio){const w=Math.max(320,canvas.clientWidth||760),h=Math.max(390,Math.round(w/ratio)),dpr=Math.min(2,devicePixelRatio||1);if(canvas.width!==Math.round(w*dpr)||canvas.height!==Math.round(h*dpr)){canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr)}const ctx=canvas.getContext("2d");ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);return{ctx,w,h}}
function label(ctx,text,x,y,color="#173f5b",align="left",size=13){ctx.save();ctx.font=`800 ${size}px Arial`;ctx.textAlign=align;ctx.lineWidth=4;ctx.strokeStyle="rgba(255,255,255,.96)";ctx.strokeText(text,x,y);ctx.fillStyle=color;ctx.fillText(text,x,y);ctx.restore()}
function arrow(ctx,a,b,color,width=3){ctx.save();ctx.strokeStyle=color;ctx.fillStyle=color;ctx.lineWidth=width;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();const t=Math.atan2(b.y-a.y,b.x-a.x);ctx.beginPath();ctx.moveTo(b.x,b.y);ctx.lineTo(b.x-11*Math.cos(t-.5),b.y-11*Math.sin(t-.5));ctx.lineTo(b.x-11*Math.cos(t+.5),b.y-11*Math.sin(t+.5));ctx.closePath();ctx.fill();ctx.restore()}
function initGraphic(wrap){
 if(wrap.dataset.ready)return;wrap.dataset.ready="1";const s=LESSONS[wrap.dataset.genericLop],v=solve(s),canvas=wrap.querySelector("[data-generic-lop-canvas]"),zoom=wrap.querySelector("[data-generic-lop-zoom]"),stageOut=wrap.querySelector("[data-generic-lop-stage]"),titleOut=wrap.querySelector("[data-generic-lop-title]"),textOut=wrap.querySelector("[data-generic-lop-text]"),dots=wrap.querySelector("[data-generic-lop-dots]"),play=wrap.querySelector("[data-generic-lop-play]"),prev=wrap.querySelector("[data-generic-lop-prev]"),next=wrap.querySelector("[data-generic-lop-next]"),replay=wrap.querySelector("[data-generic-lop-replay]");
 const [a,b]=s.stars,stages=[["Situación estimada","El punto azul oscuro es la situación estimada. Todas las medidas comienzan aquí."],[`Azimut de ${a.name}`,`Traza desde la estima la dirección ${a.azLabel}.`],[`Intercepto de ${a.name}`,`Δa = ${signed(a.dh)}: ${markText(a)}.`],[`Recta de ${a.name}`,"Por la marca dibuja una perpendicular al azimut."],[`Azimut de ${b.name}`,`Vuelve a la estima y traza ${b.azLabel}.`],[`Intercepto de ${b.name}`,`Δa = ${signed(b.dh)}: ${markText(b)}.`],[`Recta de ${b.name}`,"Por la segunda marca dibuja otra perpendicular."],["Situación observada","El punto verde donde se cortan las rectas es la posición real del barco."],["Mide Norte/Sur y Este/Oeste",`El cruce queda ${moveLabel(v.north,"Norte","Sur")} y ${moveLabel(v.east,"Este","Oeste")}.`],["Pasa a coordenadas",`La latitud cambia ${comma(Math.abs(v.north).toFixed(1))}′; la longitud cambia ${comma(Math.abs(v.lonMinutes).toFixed(1))}′.`]];
 dots.innerHTML=stages.map(()=>"<span></span>").join("");let stage=0,phase=1,playing=false,timer=0,frame=0,ox=0,oy=0,drag=false,lastX=0,lastY=0;const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches,progress=i=>stage>i?1:stage===i?phase:0;
 function panel(){stageOut.textContent=`PASO VISUAL ${stage+1} DE ${stages.length}`;titleOut.textContent=stages[stage][0];textOut.textContent=stages[stage][1];[...dots.children].forEach((d,i)=>d.className=i<stage?"done":i===stage?"current":"");prev.disabled=stage===0;next.disabled=stage===stages.length-1;play.textContent=playing?"❚❚ Pausar":stage===stages.length-1?"▶ Ver de nuevo":"▶ Ver explicación"}
 function draw(){const narrow=(canvas.clientWidth||760)<520,{ctx,w,h}=canvasSize(canvas,narrow?.86:1.5),scale=Math.min(Number(zoom.value),narrow?28:Number(zoom.value)),cx=w*.5+ox,cy=h*(narrow?.61:.51)+oy,toP=(e,n)=>({x:cx+e*scale,y:cy-n*scale}),max=13;ctx.fillStyle="#fff";ctx.fillRect(0,0,w,h);ctx.strokeStyle="#dfe8ed";ctx.lineWidth=1;for(let i=-max;i<=max;i++){let p=toP(i,-max),q=toP(i,max);ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke();p=toP(-max,i);q=toP(max,i);ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}arrow(ctx,toP(0,0),toP(0,6),"#173f5b");label(ctx,"N",toP(0,6).x,toP(0,6).y-8,"#173f5b","center",15);label(ctx,"OESTE ←",16,h-16,"#657684");label(ctx,"→ ESTE",w-16,h-16,"#657684","right");
  const drawStar=(star,color,azStage,markStage,lineStage)=>{const r=star.az*Math.PI/180,u={e:Math.sin(r),n:Math.cos(r)},normal={e:Math.cos(r),n:-Math.sin(r)},origin=toP(0,0),azTip=toP(u.e*7,u.n*7),markWorld={e:u.e*star.dh,n:u.n*star.dh},mark=toP(markWorld.e,markWorld.n),pa=progress(azStage),pm=progress(markStage),pl=progress(lineStage);if(pa>0){const tip={x:origin.x+(azTip.x-origin.x)*pa,y:origin.y+(azTip.y-origin.y)*pa};ctx.save();ctx.globalAlpha=.48;ctx.setLineDash([7,6]);arrow(ctx,origin,tip,color,2);ctx.restore();if(pa>.6)label(ctx,`${star.name} · Zv ${star.azLabel}`,tip.x+(u.e<0?-8:8),tip.y+22,color,u.e<0?"right":"left",12)}if(pm>0){const end={x:origin.x+(mark.x-origin.x)*pm,y:origin.y+(mark.y-origin.y)*pm};arrow(ctx,origin,end,color,3);ctx.fillStyle=color;ctx.beginPath();ctx.arc(mark.x,mark.y,7*Math.min(1,pm*1.5),0,Math.PI*2);ctx.fill();if(pm>.6)label(ctx,`Δa ${signed(star.dh)} · MARCA AQUÍ`,mark.x+(markWorld.e<0?-10:10),mark.y-12,color,markWorld.e<0?"right":"left",12)}if(pl>0){const ext=12*pl,p1=toP(markWorld.e-normal.e*ext,markWorld.n-normal.n*ext),p2=toP(markWorld.e+normal.e*ext,markWorld.n+normal.n*ext);ctx.strokeStyle=color;ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(p1.x,p1.y);ctx.lineTo(p2.x,p2.y);ctx.stroke();if(pl>.72){const tp=toP(markWorld.e+normal.e*(star===a?5:-5),markWorld.n+normal.n*(star===a?5:-5)),ang=Math.atan2(p2.y-p1.y,p2.x-p1.x);ctx.save();ctx.translate(tp.x,tp.y);ctx.rotate(ang>Math.PI/2||ang<-Math.PI/2?ang+Math.PI:ang);label(ctx,`${star.name.toUpperCase()} · RECTA DE ALTURA`,0,-8,color,"center",12);ctx.restore()}}};
  drawStar(a,"#2878b5",1,2,3);drawStar(b,"#e07816",4,5,6);const est=toP(0,0);ctx.fillStyle="#173f5b";ctx.beginPath();ctx.arc(est.x,est.y,7,0,Math.PI*2);ctx.fill();label(ctx,"SITUACIÓN ESTIMADA",est.x+10,est.y-12,"#173f5b");const pf=progress(7);if(pf>0){const obs=toP(v.east,v.north);ctx.save();ctx.globalAlpha=Math.min(1,pf*2);ctx.setLineDash([6,5]);ctx.strokeStyle="#198754";ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(est.x,est.y);ctx.lineTo(obs.x,obs.y);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle="#198754";ctx.beginPath();ctx.arc(obs.x,obs.y,9,0,Math.PI*2);ctx.fill();label(ctx,"AQUÍ ESTÁ REALMENTE EL BARCO",obs.x+12,obs.y+24,"#176b43");label(ctx,"SITUACIÓN OBSERVADA",obs.x+12,obs.y+42,"#176b43");ctx.restore();const pm=progress(8);if(pm>0){const corner=toP(0,v.north);ctx.save();ctx.globalAlpha=pm;ctx.strokeStyle="#7657a5";ctx.lineWidth=3;ctx.setLineDash([5,4]);ctx.beginPath();ctx.moveTo(est.x,est.y);ctx.lineTo(corner.x,corner.y);ctx.lineTo(obs.x,obs.y);ctx.stroke();ctx.setLineDash([]);label(ctx,`${comma(Math.abs(v.north).toFixed(1))} MN ${v.north>=0?"N":"S"}`,corner.x+9,(est.y+corner.y)/2,"#493270");label(ctx,`${comma(Math.abs(v.east).toFixed(1))} MN ${v.east>=0?"E":"W"}`,(corner.x+obs.x)/2,corner.y-10,"#493270","center");ctx.restore()}const pc=progress(9);if(pc>0){const bw=narrow?w-16:Math.min(330,w*.46),bx=narrow?8:w-bw-12;ctx.save();ctx.globalAlpha=pc;ctx.fillStyle="rgba(255,255,255,.97)";ctx.strokeStyle="#7657a5";ctx.lineWidth=2;ctx.beginPath();ctx.roundRect(bx,10,bw,78,9);ctx.fill();ctx.stroke();label(ctx,`LATITUD: ${comma(Math.abs(v.north).toFixed(1))} MN = ${comma(Math.abs(v.north).toFixed(1))}′`,bx+10,36,"#493270","left",narrow?10:12);label(ctx,`LONGITUD: ${comma(Math.abs(v.east).toFixed(2))} ÷ cos(latitud)`,bx+10,58,"#195785","left",narrow?10:12);label(ctx,`= ${comma(Math.abs(v.lonMinutes).toFixed(1))}′`,bx+10,78,"#195785","left",narrow?10:12);ctx.restore()}}}
 function stop(){playing=false;clearTimeout(timer);cancelAnimationFrame(frame);panel()}
 function go(nextStage,animate=true){clearTimeout(timer);cancelAnimationFrame(frame);stage=Math.max(0,Math.min(stages.length-1,nextStage));phase=animate&&!reduced?0:1;panel();draw();if(phase===1){if(playing&&stage<stages.length-1)timer=setTimeout(()=>go(stage+1,true),1300);else if(stage===stages.length-1)stop();return}const start=performance.now(),tick=now=>{phase=Math.min(1,(now-start)/850);draw();if(phase<1)frame=requestAnimationFrame(tick);else if(playing&&stage<stages.length-1)timer=setTimeout(()=>go(stage+1,true),1300);else if(stage===stages.length-1)stop()};frame=requestAnimationFrame(tick)}
 play.onclick=()=>{if(playing){stop();return}if(stage===stages.length-1){stage=0;phase=1}playing=true;panel();draw();timer=setTimeout(()=>go(stage+1,true),500)};prev.onclick=()=>{stop();go(stage-1,false)};next.onclick=()=>{stop();go(stage+1,true)};replay.onclick=()=>{stop();stage=0;phase=1;playing=true;panel();draw();timer=setTimeout(()=>go(1,true),500)};zoom.oninput=draw;canvas.onwheel=e=>{e.preventDefault();zoom.value=Math.max(+zoom.min,Math.min(+zoom.max,+zoom.value+(e.deltaY<0?2:-2)));draw()};canvas.onpointerdown=e=>{drag=true;lastX=e.clientX;lastY=e.clientY;canvas.setPointerCapture(e.pointerId)};canvas.onpointermove=e=>{if(!drag)return;ox+=e.clientX-lastX;oy+=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;draw()};canvas.onpointerup=()=>drag=false;wrap.querySelector("[data-generic-lop-reset]").onclick=()=>{ox=oy=0;zoom.value=38;draw()};new ResizeObserver(draw).observe(canvas);panel();draw();
}

window.initReclassifiedGraphics=function(root=document){baseGraphics?.(root);root.querySelectorAll("[data-generic-lop]").forEach(initGraphic)};
})();
